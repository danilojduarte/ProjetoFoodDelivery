import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    icone: {
        width: 220,
        height: 120,
        borderRadius: 10,
    },
    banner: {
        paddingRight: 10,
        marginTop: 15,
        marginBottom: 10
    },
    descricao: {
        fontSize: FONT_SIZE.xsm,
        color: COLORS.dark_gray,
        textAlign: "center"
    }
}